# Agent Builder Demo

This is a demo NextJS app that is an agent builder platform. It allows you to create custom agents with access to specific tool calls.

All agent interactions are traced through Braintrust. The evals/ dir houses a simple remote eval server.